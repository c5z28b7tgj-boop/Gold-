name: Gold Alert Bot

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  check-gold:
    runs-on: ubuntu-latest

    steps:
      - name: Atsisiųsti kodą
        uses: actions/checkout@v4

      - name: Paleisti Python
        run: |
          pip install requests
          python gold_bot.py
